# Plausible analytics for Gridsome

## Install

- `npm install gridsome-plugin-plausible`
- `yarn add gridsome-plugin-plausible`

## Usage

```js
module.exports = {
  plugins: [
    {
      use: 'gridsome-plugin-plausible'
    }
  ]
}
```
